# Phase 6: Local Daily Completion

Date: 2026-09-17. P6.1-P6.4 are complete for local gameplay, with an indefinite rotation of 56 unique themes. The user selected **Mid IQ** as the Daily launch mode. Public ranking, authenticated attempts and trusted deadlines remain Phase 8.

## Implemented Contract

- New `daily-3` seeds pin UTC date, Mid IQ, IQ/engine/data/random/score/rivalry versions plus `rotation-1` and challenge ID. Existing `daily-1` saves and ledger results retain their original unrestricted pools, three offers and seeds; `daily-2` retains its finite `calendar-1`/`calendar-2` mapping and exact seeds. Ordinary runs, baseline math and calibration artifacts are unchanged.
- The [rotation](../src/engine/daily-calendar.ts) starts fresh on **2026-09-17 UTC** and has no expiry. Every 56-day cycle contains each of the 56 themes once, including a shuffled first cycle (September 17-November 11). Cycle number is the floor of elapsed UTC days divided by 56; the remainder selects the day. IDs are sorted by code point, then Fisher-Yates shuffled using the existing versioned FNV1a/Mulberry32 stream with rotation version, anchor and cycle number. If the first theme equals the previous raw cycle's last theme, swap the first two. Since this never changes the last theme of a 56-item cycle, the boundary guarantee needs only two shuffles, without recursion or stored history. Each date retains a distinct draft seed. The retired published schedule exists only for legacy saves. Pool definitions and shuffle rules are part of the versioned contract: future changes must preserve replay for existing versions.
- Fixed-coach days offer only the named coach, still requiring signing. Other days retain three seeded offers. Era/franchise/stat/position restrictions apply to priorities, spins, rerolls, candidates and pick validation. Fixed-player days place one or two exact franchise/era records in specified slots before coach signing, leaving five or four manual picks and excluding all other versions of those people. Locked players are exceptions to remaining-pool restrictions. Duplicate fixed identities/slots and illegal placements are rejected. Locked slots cannot be replaced. The next roll uses the existing occupied-slot round number.
- Stat restrictions use raw record averages before pace normalization or coach effects: PPG < 20, USG% <= 20, 3PA < 1 and USG% >= 25. No Refunds starts with zero tokens on both axes. The centers-only challenge requires C eligibility and locks Raptors 2020s Scottie Barnes at PG, leaving five legal picks. These are theme restrictions, not new simulation rules; historical themes do not recreate exact historical squads.
- Sorted canonical franchise/decade sets from the restricted pool are shuffled in independent round/action/axis streams. The first legal franchise, then first legal decade within it, wins. Team/era rerolls retain the opposite axis, must change the requested axis and keep the token when no alternative exists. Single-era days retain their unused era token, with the control disabled. Earlier picks can require different legal fallbacks.
- Daily rolls never advance the sequential draft random state. Schedule and outcome/score streams remain independently seeded by the challenge, not attempt IDs or actions. Unique attempt IDs distinguish local/practice saves without changing their challenge draws.
- `RunSave.daily` retains date, version, first-local/practice kind, start time and attempt ID; `daily-2`/`daily-3` also require calendar version and challenge ID. Recovery reconstructs initial restrictions and locked players, replays the legal action history and rejects inconsistent challenge/date/mode/version/seed metadata. Mode changes cannot modify Daily runs.
- `98-0-daily-v1` stores commitments and regular-season summaries separately from the active run. Web Locks serialize starts and result writes. Commitment precedes revealing offers; corrupt/full/unavailable storage blocks new Daily entry without clearing records or replacing the active run.
- One first-local rotation attempt per date is tracked in this browser profile. Legacy commitments remain grouped separately, permitting a fresh rotation attempt on the restart date without clearing records. Abandoning retains the commitment; retries are practice. An active unfinished Daily, including a legacy one, resumes with its original date across midnight. New dates never automatically replace active runs.
- Regular-season completion is recorded independently of postseason and playback. The strict receipt deadline is UTC midnight two days after the challenge date; equality is late. Practice/late records are unranked. Results list date, Mid IQ, wins/losses, differential and longest winning streak, with local-only labels. The active record does not reveal totals before all 82 playback results are visible.
- The ranking comparator refuses different challenges/modes and uses wins, differential, then longest streak descending. Exact ties ignore completion time. The UI is a chronological personal record list, not a public ranking.
- The Daily dialog shows the current theme, restriction and expandable current 56-day cycle. Draft/result banners and local records retain the theme. Reel animation, coach pool counts and reroll availability reflect the restricted pool. An open dialog refreshes its UTC date and cycle; a date change during start requires reopening instead of silently accepting an unseen challenge.

## Draftability Guarantee

Every challenge is checked against the bundled data before starting. After excluding all fixed identities and occupied slots, each remaining slot must have at least as many distinct eligible identities as there are remaining slots. This sufficient condition is intentionally conservative: after any legal pick, at most one eligible identity disappears from each slot and the required slot count decreases by one. Therefore every legal sequence retains enough distinct candidates to complete the roster; first-legal roll fallback can always offer one. All 56 pools pass. Centers-only without a locked PG fails: five PG identities can all be placed elsewhere. Locking Scottie Barnes at PG leaves at least 133 distinct candidates for each remaining slot. This is a legality guarantee, not a guarantee of good roster quality or favorable results.

## Verification

Existing Node and Playwright suites were reused; no backend, database or Docker infrastructure is required for this local-only scope. Browser tests use installed Edge against a dedicated production Next.js server, at 1440x1000 and 320x800.

| Check | Result | Exit Code |
| --- | --- | --- |
| `npm run typecheck` | Pass | 0 |
| `npm run build` | Pass; static application generated | 0 |
| `node --experimental-strip-types --test src/engine/daily.test.ts src/engine/run.test.ts` | 30 passed, 0 failed, 0 skipped | 0 |
| `npm run test:browser -- tests/browser/daily.spec.ts` | 38 passed, 0 failed | 0 |

The rotation update reused the existing test files. Engine checks add 200 complete cycles, uniqueness, boundary-repeat prevention, distant/leap dates, UTC offset agreement, fresh seeds, legacy commitment separation and complete save replay across cycle boundaries. Browser checks now locate themes in the new schedule, keep all restriction/draft journeys, verify open-dialog cycle rollover at UTC midnight and prove legacy same-day saves coexist with a fresh first attempt. Two independent profiles in Los Angeles and Tokyo, with networking disabled after load, produce identical future-date themes, seeds, coach offers and first reels. Desktop/mobile current-cycle screenshots were inspected and overflow checks pass. Production startup and all focused checks passed; unrelated full browser suites were not rerun for this rotation update.

### Earlier Validation

Before the rotation update, `npm test` passed 108 tests and the full browser suite passed 72 tests, including 34 Daily checks (all exit code 0). The notes below describe those earlier finite-calendar releases; their expiry assertions have now been replaced with rotation boundary checks.

New [engine tests](../src/engine/daily.test.ts) cover UTC date validation, strict grace boundaries, action/round priority independence, dataset-order independence, legal six-pick replay including an early sixth man, both reroll axes, unavailable rerolls, schedule independence, locked mode, save metadata rejection and comparison ties.

New [browser tests](../tests/browser/daily.spec.ts) cover commitment before offers, interactive six-pick completion, reloads, retained summaries, identical practice offers, UTC rollover/resume, abandonment, next-day entry, late completion and corrupt/full-storage failure. Existing IQ-mode, audio, postseason, rivalry and save-recovery checks remain green. Desktop/mobile Daily screenshots were inspected; document overflow checks pass. Initial browser failures were corrected by awaiting the asynchronous start transition and narrowing an alert selector; no failing checks were waived.

The unique-calendar extension adds distinct restriction signatures, exact fixed-player references, conservative completion coverage, no-wrap date boundaries, all 28 themes with two complete draft/season replay paths each, legal rerolls, locked slots, duplicate protection, metadata rejection and legacy compatibility. Browser journeys add fixed-coach presentation, single-era and two-franchise pools, five-pick fixed-center/fixed-bench drafts, all 28 calendar entries and expiry with last-day resume. Production startup, engine and browser tiers all passed with exit code 0. No infrastructure tier applies; Node 24.14.0 and Edge/Playwright were available. Screenshots cover fixed-coach, restricted draft and expired-calendar layouts. These checks do not establish difficulty balance or server trust.

The second-calendar extension expands the same engine replay test to all 56 themes with two complete draft/season paths each. Added checks cover exact stat boundaries, raw eligibility filters, duplicate fixed-player/slot rejection, reroll allowance validation, first-calendar seed stability and wrong-calendar rejection. Eight new browser scenarios per viewport cover Kobe with Charlotte, Shaq plus Curry (including season results and practice replay), all four stat filters, zero rerolls and the corrected centers-only finale. Calendar expiry now tests November 11 with last-day resume. Screenshots of the duo draft, centers-only mobile draft and expanded calendar were inspected; overflow checks pass. All runtime gates above pass; difficulty calibration and human playtesting remain unverified.

## Limits

- Local storage, device clocks and downloaded seeds are user-controlled. No trusted accounts, server receipt, tamper resistance, cross-device recovery or public leaderboard is implemented. Clearing browser data removes commitments/results. Web Locks require HTTPS or localhost and browser support.
- Offline synchronization assumes the same app/data/rotation versions and correct UTC device dates. No network request or local progression counter is needed to calculate themes. Offline installation, service-worker caching and cold-launch availability are outside this change.
- There is still one active run save. Starting another run discards its draft/playback, but not Daily commitments or recorded summaries. General 50-run history, postseason summary archives, Almanac unlocks and sharing belong to Phase 7.
- Functional validation is not Daily difficulty calibration, mode-balance acceptance, a human playtest, a complete accessibility audit or Safari/physical-mobile verification.